# _development/_review

**Choose this use case (in addition to `_development`) when** the agent being
created will review code rather than primarily write it — pull-request review,
code quality, and security review of changes. Recognition signals: "review",
"PR review", "code quality", "audit a change", "reviewer agent".

## What it provides

- Skills: `code-review-skill` (git submodule).
